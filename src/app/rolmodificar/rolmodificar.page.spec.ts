import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RolmodificarPage } from './rolmodificar.page';

describe('RolmodificarPage', () => {
  let component: RolmodificarPage;
  let fixture: ComponentFixture<RolmodificarPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RolmodificarPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
